$(document).ready(function() {
    $("h2").addClass("underline"); //underlines all <h2> elements
    $("ul").addClass("border"); //adds a border to all unordered lists
    $("tr:odd").addClass("odd"); //adds orange background to odd rows
    $("tr:even").addClass("even"); //adds orange background to even rows
});